import { withInstall } from '@/utils';
import _VirtualScroller from './VirtualScroller';
import './index.less';

export const OVirtualScroller = withInstall(_VirtualScroller);
export default OVirtualScroller;

