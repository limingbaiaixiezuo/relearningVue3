import { withInstall } from '@/utils';
import _ContextMenu from './ContextMenu';

export const OContextMenu = withInstall(_ContextMenu);
export default OContextMenu;
export * from './functionCall';
import './index.less';

